<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.notesforhimani">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".xylophone"></activity>
        <activity
            android:name=".Setting"
            android:windowSoftInputMode="adjustResize" />
        <activity
            android:name=".DisplayActivity"
            android:windowSoftInputMode="adjustResize" />
        <activity
            android:name=".AddNotes"
            android:windowSoftInputMode="adjustResize" />
        <activity
            android:name=".authenticationPassword"
            android:windowSoftInputMode="stateVisible|adjustResize" />
        <activity android:name=".readhere" />
        <activity android:name=".SplashS">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
        <service android:name=".BackgroundSound" android:enabled="true"/>
    </application>

</manifest>